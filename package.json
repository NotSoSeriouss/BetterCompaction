{
  "name": "better-compaction",
  "version": "0.1.0",
  "description": "Context compaction for pi — summarizes by appending the compaction instruction to the real conversation, so the backend's prompt cache is reused",
  "keywords": [
    "pi-package",
    "pi",
    "pi-extension",
    "compaction",
    "context",
    "cache"
  ],
  "license": "MIT",
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  }
}
